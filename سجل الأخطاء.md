# سجل الأخطاء والمشكلات - نظام إدارة البرامج الشهرية

## 1. خطأ `requireValueInSheet is not a function`
- **التاريخ:** 2026-07-07
- **السياق:** عند الضغط على "🔧 تهيئة وربط" في تبويب Google Sheets
- **الرسالة:** `TypeError: SpreadsheetApp.newDataValidation(...).requireValueInSheet is not a function`
- **السبب:** Google أزال دالة `requireValueInSheet` من Apps Script
- **الحل:** استبدال `requireValueInSheet(...)` بـ `requireValueInRange(..., true)`

---

## 2. فشل بناء GitHub Pages (404)
- **التاريخ:** 2026-07-07
- **السياق:** فتح رابط الموقع `jamahjalyat-ctrl.github.io/-/10.html`
- **الرسالة:** `Failed to load resource: the server responded with a status of 404`
- **السبب:** GitHub Pages حاول بناء الموقع كـ Jekyll وفشل
- **الحل:** إضافة ملف `.nojekyll` فارغ في جذر المستودع لتعطيل Jekyll

---

## 3. البيانات لا تظهر من جهاز آخر
- **التاريخ:** 2026-07-07
- **السياق:** فتح الموقع من جهاز/متصفح ثاني
- **الظاهرة:** عداد البرامج 0 أو أقل من الجهاز المحلي
- **السبب الأول:** التطبيق لا يقرأ من GS تلقائياً عند البدء
  - **الحل:** إضافة `loadStateFromGs()` في `initApp()` قبل التحميل
- **السبب الثاني:** رابط Web App قديم (بعد إعادة النشر)
  - **الحل:** تحديث رابط Web App في التطبيق المحلي والموقع
- **السبب الثالث:** المزامنة لم ترفع كل البيانات
  - **الحل:** الضغط على "🔄 مزامنة الآن" ثم الانتظار

---

## 4. اختفاء القيم من القوائم المنسدلة أثناء التحديث
- **التاريخ:** 2026-07-07 (سابق)
- **السياق:** التحديث الدوري كل 60 ثانية
- **الظاهرة:** القوائم (البرامج، الجاليات، إلخ) تفرغ وتعيد التعبئة فتفقد القيم المختارة
- **السبب:** إعادة بناء HTML القوائم دون حفظ القيم الحالية
- **الحل:** دوال `refreshSelectors()` و `populateApSelectors()` تحفظ القيم قبل إعادة البناء وتستعيدها بعده

---

## 5. اختفاء البيانات بعد التحديث الدوري (60s Interval)
- **التاريخ:** 2026-07-07 (سابق)
- **السياق:** التحديث الدوري كل 60 ثانية للتصنيفات من GS
- **الظاهرة:** مسح البيانات المحلية (برامج، جداول) بعد التحديث
- **السبب:** الـ interval كان يستبدل `state` بالكامل بدلاً من دمج التصنيفات فقط
- **الحل:** تغيير الـ interval ليدمج التصنيفات الجديدة فقط (push if not exists) بدلاً من استبدال state

---

## 6. خطأ أثناء استيراد Excel: عمود غير موجود
- **التاريخ:** 2026-07-07 (سابق)
- **السياق:** رفع ملف Excel في قسم استيراد البرامج
- **الرسالة:** "لم يتم العثور على عمود 'اسم البرنامج'"
- **السبب:** الملف لا يحتوي على الأعمدة المتوقعة
- **الحل:** البحث بأسماء بديلة (اسم البرنامج، البرنامج، name) والتأكد من وجود قالب تحميل

---

## 7. مشكلة تاريخ الملفات المكررة في الطباعة
- **التاريخ:** 2026-07-07 (سابق)
- **السياق:** طباعة قائمة البرامج والبطاقات
- **الظاهرة:** تكرار نفس البرنامج عدة مرات بدلاً من تجميعه
- **السبب:** الطباعة كانت تكرر البرنامج لكل تاريخ دون تجميع
- **الحل:** تعديل 14: تجميع البرامج (صف واحد + عمود تاريخ وعدد مرات)

---

## 8. اختفاء بيانات الإدخال أثناء التعبئة
- **التاريخ:** 2026-07-07 (سابق)
- **السياق:** أثناء إدخال بيانات برنامج جديد أو تسكينه
- **الظاهرة:** اختفاء القيم المدخلة فجأة
- **السبب:** الـ 60s interval يعيد بناء القوائم فتفقد القيم
- **الحل:** استخدام متغيرات لحفظ القيم (`savedComm` للخطة السنوية، و`saved` للقوائم العامة)

---

## 9. زر الخطة السنوية يعود إلى "الكل" بعد الحفظ
- **التاريخ:** 2026-07-07 (سابق)
- **السياق:** حفظ برنامج في الخطة السنوية
- **الظاهرة:** فلتر الجالية يعود تلقائياً إلى "الكل"
- **السبب:** استدعاء `renderAnnualPlan()` يعيد بناء الفلتر
- **الحل:** حفظ قيمة الفلتر في متغير `savedComm` قبل `renderAnnualPlan()` وإعادته بعده
